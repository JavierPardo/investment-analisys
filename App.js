import { SafeAreaView, StyleSheet, Text, View } from "react-native";
import { AnalisysView } from "./src/views";

export default function App() {
  return (
    <SafeAreaView>
      <AnalisysView />
    </SafeAreaView>
  );
}
